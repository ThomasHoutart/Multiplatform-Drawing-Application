package com.android.example.drawhubmobile.models.event

enum class EventTypes {
    // General
    INTERNAL_SERVER_ERROR,
    NOT_LOGGED_IN_ERROR,
    NO_CONTENT_ERROR,
    BAD_CONTENT_ERROR,
    PERMISSION_ERROR,

    // Login/Logout
    USER_LOGIN,
    LOGIN_SEND,
    USER_AUTHENTICATED,
    LOGOUT_SEND,
    USER_DOES_NOT_EXIST_ERROR,
    BAD_PASSWORD_ERROR,
    ALREADY_LOGGED_IN_ERROR,
    USER_CHEATED_ERROR,

    // Chat
    CHAT_MESSAGE_SEND,
    CHAT_MESSAGE_RECEIVED,
    BLOCK_CHAT,

    // Chat Rooms
    CREATE_ROOM_SEND,
    JOIN_ROOM_SEND,
    LEAVE_ROOM_SEND,
    DELETE_ROOM_SEND,
    CREATE_ROOM_RECEIVED,
    JOIN_ROOM_RECEIVED,
    LEAVE_ROOM_RECEIVED,
    DELETE_ROOM_RECEIVED,
    NOT_IN_ROOM_ERROR,
    ROOM_DOES_NOT_EXIST_ERROR,
    ROOM_ALREADY_EXISTS_ERROR,
    MAX_ROOMS_JOINED_ERROR,
    ALREADY_IN_ROOM_ERROR,

    // Lobby
    CREATE_LOBBY_SEND,
    DELETE_LOBBY_SEND,
    JOIN_LOBBY_PLAYER_SEND,
    JOIN_LOBBY_PLAYER_RECEIVED,
    LEAVE_LOBBY_PLAYER_SEND,
    LEAVE_LOBBY_PLAYER_RECEIVED,
    JOIN_LOBBY_SPECTATOR_SEND,
    JOIN_LOBBY_SPECTATOR_RECEIVED,
    LEAVE_LOBBY_SPECTATOR_SEND,
    LEAVE_LOBBY_SPECTATOR_RECEIVED,
    CREATE_LOBBY_RECEIVED,
    DELETE_LOBBY_RECEIVED,
    UPDATE_LOBBY,
    LOBBY_INFO,
    ADD_BOT,
    REMOVE_PLAYER,
    LOBBY_DOES_NOT_EXIST_ERROR,
    LOBBY_ALREADY_EXISTS_ERROR,
    LOBBY_FULL_ERROR,
    ALREADY_IN_LOBBY_ERROR,
    USER_NOT_IN_LOBBY_ERROR,
    NOT_ENOUGH_PLAYERS_ERROR,

    // Game
    START_GAME_SEND,
    START_GAME_RECEIVED,
    END_GAME,
    START_ROUND,
    END_ROUND,
    LEAVE_GAME_PLAYER_SEND,
    LEAVE_GAME_PLAYER_RECEIVED,
    JOIN_GAME_SPECTATOR_SEND,
    JOIN_GAME_SPECTATOR_RECEIVED,
    LEAVE_GAME_SPECTATOR_SEND,
    LEAVE_GAME_SPECTATOR_RECEIVED,
    HINT_SEND,
    HINT_RECEIVED,
    GAME_INFO,
    WORD_FOUND,
    GAME_TICK,
    ELIMINATED,
    WORD_TO_DRAW,
    USER_CHEATED,

    // Drawing
    SET_PATH_SEND,
    SET_PATH_RECEIVED,
    APPEND_TO_PATH_SEND,
    APPEND_TO_PATH_RECEIVED,
    NOT_ARTIST_ERROR,
    UPDATE_DRAWING_CANVAS,
    CLEAR_DRAWING_CANVAS,
}